export const MANIFEST_FILE_NAME = 'ext.manifest.yml';

export const ENTRY_DRAW_EDITOR = 'src/editor';
export const ENTRY_DRAW_ATTR = 'src/attr';
export const ENTRY_DRAW_READER = 'src/reader';

export const ENTRY_BASE = 'src/main';

export const BUNDLE_DIR = 'dist';

export const BUNDLE_MANIFEST_NAME = 'ext.manifest.json';

export const CUSTOM_WEBPACK_CONFIG = 'webpack.config.merge.js';
